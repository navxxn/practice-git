#create a file in the provided path

# param(
#     [parameter(Mandatory, HelpMessage = 'Enter the path')]
#     [string]$path
# )

# New-Item -path $path
# Write-Host "file created in the given path"

$data = Get-Date -format 'MM-dd'

compress-